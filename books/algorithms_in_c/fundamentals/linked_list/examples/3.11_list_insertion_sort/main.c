#include <stdlib.h>
#include <stdio.h>
#include <math.h>

typedef struct Node* Link;
struct Node {
    int item;
    Link next;
};

Link generate_random_list(int len) {
   
}

int main() {
    Link h;
    printf("%x", h);
    return 0;
}